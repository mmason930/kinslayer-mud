var script625 = function(self, actor, here, args, extra) {
	// Alder
	// May 2010
	_block;
	actor.detach(625);
	var vTitles = actor.titles;
	var title = vTitles[vTitles.length-1];
	actor.setSpecTitle(title);
	return;
}