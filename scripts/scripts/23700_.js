var script23700 = function(self, actor, here, args, extra) {
	//Maerwynn smob zone echo.
	//Koradin - December 2009
	if (random(0,10) == 1) {
	here.zecho("Shrill cackling can be heard throughout the area.");
	}
}