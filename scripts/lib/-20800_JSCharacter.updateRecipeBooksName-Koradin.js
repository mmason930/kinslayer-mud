//Updates a character's recipe book
//Koradin - May 2010
// JSCharacter.prototype.updateRecipeBook = function(iVnum, sType)
// {
	//Update Cooking or Mixing Recipe
	// var already_has_recipe = false;
	// if (this.getPval(sType.toLowerCase()+"_recipes"))
	// {
		// for (var _autoKey in this.getPval(sType.toLowerCase()+"_recipes").split("~")) { 	var Recipe = this.getPval(sType.toLowerCase()+"_recipes").split("~")[_autoKey];
		// {	
 }
			// if (parseInt(Recipe) == iVnum)
				// already_has_recipe = true;
		// }
		// if (already_has_recipe == false)
			// this.setPval(sType.toLowerCase()+"_recipes",this.getPval(sType.toLowerCase()+"_recipes")+"~"+iVnum,true);
	// }
	// else
		// this.setPval(sType.toLowerCase()+"_recipes",iVnum,true);
	//Update Total Recipes	
	// if (already_has_recipe == false)
	// {
		// if (this.getPval("total_recipes"))
			// this.setPval("total_recipes",this.getPval("total_recipes")+"~"+iVnum,true);
		// else
			// this.setPval("total_recipes",iVnum,true);
	// }
// }
