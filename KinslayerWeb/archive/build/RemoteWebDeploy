#!/bin/bash

(

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR/../..

rsync -avz --exclude "*.svn" ./ kin:/kinslayer/control/trunk/KinslayerWeb/

ssh kin 'cd /kinslayer/control/ && ./DeployDevWeb'

)