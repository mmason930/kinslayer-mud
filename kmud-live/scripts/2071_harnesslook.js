var script2071 = function(self, actor, here, args, extra)
{
	//EXAMINE, LOOK AT, LOOK IN a harness
//Koradin - April 2010
actor.send("Harnesses have been temporarily disabled. Please check back later.");
// var arg_array = getArgList(args);
// if (arg_array.length < 2 || arg_array.length > 3)
// {
	// _noblock;
	// return;
// }
// if (arg_array.length == 3)
// {
	// if (arg_array[0] == "l" || arg_array[0] == "lo" || arg_array[0] == "loo" || arg_array[0] == "look")
	// {
		// if (arg_array[1] == "i" || arg_array[1] == "a" || arg_array[1] == "in" || arg_array[1] == "at")
		// {
			// for each(var word in self.namelist.split(" "))
			// {
				// if (word == arg_array[2])
					// var trigger = 1;
			// }
		// }
		// else
			// _noblock;
	// }
	// else
		// _noblock;
// }
// else if (arg_array.length == 2)
// {
	// if (arg_array[0] == "exa" || arg_array[0] == "exam" || arg_array[0] == "exami" || arg_array[0] == "examin" || arg_array[0] == "examine")
	// {
		// for each(var word in self.namelist.split(" "))
		// {
			// if (word == arg_array[1])
				// var trigger = 1;
		// }
	// }
	// else
		// _noblock;
// }
// if (trigger == 1)
// {
	// _block;
	// actor.send("It is " + self.name + ", in very good condition.");
	// actor.send(" ");
	// actor.send("When you look inside, you see:");
	// actor.send(" ");
	// for (i = 2001; i < 2010; i++)
	// {
		// if (actor.getPval(i.toString()))
			// actor.send(self.getPval(i.toString()));
		// else
			// actor.send(i-2000+". < Empty Slot >");
	// }
// }

};

