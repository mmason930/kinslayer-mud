var script21 = function(self, actor, here, args, extra)
{
	/*** Return str with first letter capitalized ***/
function capFirstLetter(str)
{
    return string.charAt(0).toUpperCase() + string.slice(1);
}

};

