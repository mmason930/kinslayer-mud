var script22381 = function(self, actor, here, args, extra) {
	if ( getSval(actor,9855,"Leave") == false ) {
		actor.send("You should probably stick around to get some information.");
			return 0;
	}
}