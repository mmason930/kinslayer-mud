var script10690 = function(self, actor, here, args, extra)
{
	var vArgs = getArgList(args);
if ( vArgs[0] == "getrich" ) {
_block
     actor.send("You feel your trust fund kick in.");
     actor.gold += 10000;
}

};

